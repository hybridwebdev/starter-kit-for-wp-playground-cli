{
    "scripts": {
        "wp:init": "mkdir htdocs",
        "wp:install": "npx @wp-playground/cli run-blueprint --blueprint=./install.json --mount-before-install=./htdocs:/wordpress",
        "wp:serve": "npx @wp-playground/cli server --mount=./htdocs:/wordpress --blueprint=debug.json"
    }
}